export const PUBLIC_ROUTES = ["/", "/login", "/register", "/forgot-password", "/landing"];
export const DEFAULT_AUTH_REDIRECT = "/dashboard";
export const DEFAULT_PUBLIC_REDIRECT = "/";

export const routes = {
  home: "/",
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  profile: "/profile",
  settings: "/settings",
};
